---
"happiness": minor
---

Rich markdown rendering for page content, powered by the Paris `Markdown` component. Replaces bare `ReactMarkdown` usage so every markdown element maps to a Paris component or themed element — GFM tables, strikethrough, task lists, code blocks, callout-style blockquotes, and accordion-style `details`.
