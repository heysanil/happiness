---
"happiness": patch
---

Stop setting `receipt_email` on PaymentIntents so Stripe no longer sends its built-in receipt alongside the app's own confirmation email and PDF receipt, which sent donors duplicate emails
