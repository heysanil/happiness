---
"happiness": patch
---

Replace Stoplight Elements with Scalar for the API reference at `/api`, collapsing the client component, layout, and page into a single route handler. The docs page title now uses the configured app name.
